# macOS
.DS_Store

# Node / frontend build outputs
node_modules/
dist/
*.tsbuildinfo

# Local environment and secrets: never commit credentials, keys or tokens.
.env
.env.*
*.pem
*.key
*.p12
.credentials.yaml
credentials.yaml
macos/launch-agent/launch-agent.env

# Runtime scratch and logs
tools/harness-connectivity/.tmp/
spikes/**/.tmp/
*.log

# pnpm store (localized under the project)
.pnpm-store/

# Raw README screenshots. Unmasked captures land here (real user paths, Tailscale
# device names, Remote Host IDs), so they stay local; only the composed images in
# assets/readme/ are published.
raw/
